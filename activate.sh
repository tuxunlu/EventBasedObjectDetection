#!/usr/bin/env bash
# Source this file (don't execute) to activate the project venv:
#     source activate.sh
SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
source "${SCRIPT_DIR}/EventBasedObjectDetection/bin/activate"
